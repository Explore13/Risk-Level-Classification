# ML / model
# CPU-only torch — avoids pulling 2GB CUDA build on HF Spaces
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
transformers==4.46.3
safetensors==0.4.5
accelerate==1.1.1
sentencepiece==0.2.0
huggingface-hub==0.26.5

# Language detection from audio (Whisper — reliable, audio-based)
openai-whisper==20240930
ffmpeg-python==0.2.0

# Speech-to-text transcription (Google STT — native script output)
SpeechRecognition==3.10.4

# API
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.3
python-multipart==0.0.9

# Audio processing
numpy==1.26.4

# Translation & romanization
requests==2.32.3
deep-translator==1.11.4
